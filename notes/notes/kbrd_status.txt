KBRD status summary

- The KBRD code repository is present locally.
- The failure is not caused by missing KBRD code.
- The expected data directory KBRD/data/redial is missing.
- The modified build.py does not automatically download the ReDial dataset because the download lines are commented out.
- build.py also uses hardcoded Linux DBpedia paths that do not match the current Windows environment.
- Therefore, real KBRD integration requires:
  1. raw ReDial files in KBRD/data/redial
  2. DBpedia files in a local folder
  3. patching build.py paths before preprocessing