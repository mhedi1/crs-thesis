# CRS Thesis — JupyterHub requirements
# Target: Python 3.10.12, CPU-only (no CUDA on JupyterHub)
#
# Install with:
#   pip install -r requirements-jupyter.txt
#
# Then install the spaCy model separately:
#   python -m spacy download en_core_web_sm
#
# PyTorch Geometric dependencies must be installed AFTER torch.
# Run this after pip install -r requirements-jupyter.txt:
#   pip install torch-scatter torch-sparse torch-cluster torch-spline-conv \
#       -f https://data.pyg.org/whl/torch-2.1.0+cpu.html

# ── Core ──────────────────────────────────────────────────────────────────────
requests>=2.28,<3.0
PyYAML>=6.0,<7.0
tqdm>=4.64,<5.0

# ── Numerical ─────────────────────────────────────────────────────────────────
# numpy<2.0 avoids breaking API changes introduced in numpy 2.x
numpy>=1.24,<2.0
scipy>=1.10,<2.0

# ── Graph ─────────────────────────────────────────────────────────────────────
# networkx 3.x broke several APIs that KBRD relies on; pin to 2.x
networkx>=2.6,<3.0

# ── PyTorch (CPU-only) ────────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0,<2.7.0
torchvision>=0.16.0,<0.22.0

# ── PyTorch Geometric ─────────────────────────────────────────────────────────
torch-geometric>=2.3.0,<2.8.0
# Note: torch-scatter, torch-sparse, torch-cluster, torch-spline-conv
# must be installed from the PyG wheel index (see header comment above).

# ── NLP ───────────────────────────────────────────────────────────────────────
spacy>=3.5.0,<3.9.0
nltk>=3.8,<4.0
rouge-score>=0.1.2,<0.2.0

# ── Fuzzy matching ────────────────────────────────────────────────────────────
# fuzzywuzzy needs python-Levenshtein for speed; or use rapidfuzz drop-in
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0

# ── Misc ──────────────────────────────────────────────────────────────────────
packaging>=23.0
